# weather-notes runtime + test dependencies, fully pinned ("pip freeze" style,
# resolved with pip inside python:3.12-slim).
#
# Deliberately VULNERABLE pins for the Sentinel Chain demo (see README.md for the
# OSV advisory ids observed and how to re-verify them):
#   requests==2.30.0  -> GHSA-j8r2-6x86-q33q, GHSA-9wx4-h78v-vm56,
#                        GHSA-9hjg-9r4m-mvj7, GHSA-gc5v-m9x4-r6x2 (first clean release: 2.33.0)
#   Jinja2==3.1.5     -> GHSA-cpwx-vrp4-4pq7 (fixed in 3.1.6)
#
# Every other pin is compatible with Python 3.12 and with both the vulnerable
# and the fixed versions of the two packages above.
blinker==1.9.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
Flask==3.1.3
idna==3.19
iniconfig==2.3.0
itsdangerous==2.2.0
Jinja2==3.1.5
MarkupSafe==3.0.3
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
pytest==9.1.1
requests==2.30.0
urllib3==2.7.0
Werkzeug==3.1.8
