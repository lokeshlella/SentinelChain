[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: requires external services (network, Postgres, Neo4j, Docker, Ollama); skipped unless SENTINEL_INTEGRATION=1
filterwarnings =
    ignore::DeprecationWarning
